name: graycode-skills

services:
  graycode-skills:
    build:
      context: ../../
      dockerfile: Dockerfile
    image: ghcr.io/graycodeai/graycode-skills:dev
    env_file:
      - path: ../../.env.example
        required: false
